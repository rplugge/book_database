
<table>
  <tr>
    <th>ID</th>
    <th>Name</th>
  </tr>
  <% Book.where_genre(params["id"].to_i).each do |book| %>
    <tr>
      <td><%= book.id   %></td>
      <td><%= book.name %></td>
    </tr>
  <% end %>
</table>